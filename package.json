{
  "name": "@verekia/testing-np",
  "version": "0.1.6",
  "scripts": {
    "pub": "np",
    "build": "node build.js",
    "clean": "rm built.yay",
    "test": "jest",
    "prepublishOnly": "yarn clean && yarn build",
    "postpublish": "yarn clean"
  },
  "devDependencies": {
    "@verekia/testing-np": "0.1.5",
    "jest": "24.9.0",
    "np": "5.1.0"
  }
}
