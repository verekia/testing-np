{
  "name": "@verekia/testing-np",
  "version": "1.0.0",
  "scripts": {
    "pub": "np",
    "build": "node build.js",
    "clean": "rm -f built.yay",
    "test": "jest",
    "prepublishOnly": "yarn clean && yarn build",
    "postpublish": "yarn clean"
  },
  "devDependencies": {
    "@verekia/testing-np": "1.0.0",
    "jest": "24.9.0",
    "np": "5.1.0"
  }
}
